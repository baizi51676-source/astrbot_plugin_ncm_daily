{
  "music_u_cookie": {
    "description": "网易云 MUSIC_U Cookie",
    "hint": "浏览器登录 music.163.com 后按 F12，在 Network（网络）面板中复制任意请求的 Cookie 里的 MUSIC_U 值（只填值本身，不带 MUSIC_U= 前缀）。用于获取每日推荐和个人歌单；搜索音乐无需此配置。",
    "type": "string",
    "default": ""
  },
  "admin_user_ids": {
    "description": "管理员 QQ 列表（我的歌单、歌单详情、每日推荐仅管理员可用）",
    "hint": "填写 QQ 号（字符串数组），如 [\"10001\", \"10002\"]。不填则无人可查看歌单/日推（搜索和点歌不受影响）。",
    "type": "array",
    "item_type": "string",
    "default": []
  },
  "point_song_allowlist": {
    "description": "点歌白名单 QQ 列表",
    "hint": "填写 QQ 号（字符串数组），如 [\"10001\"]。留空表示不限制（所有人都可以点歌）；管理员始终可以点歌。",
    "type": "array",
    "item_type": "string",
    "default": []
  }
}